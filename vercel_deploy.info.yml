name: Vercel Deploy
description: Allow Vercel Vercel deployments integration with the corresponding permission set.
package: Utility
type: module
core_version_requirement: ^10.5 || ^11

dependencies:
  - drupal:toolbar

configure: vercel_deploy.settings
